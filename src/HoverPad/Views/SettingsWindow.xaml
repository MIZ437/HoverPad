<Window x:Class="HoverPad.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HoverPad 設定"
        Width="900" Height="750"
        MinWidth="800" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Background="#1E1E1E">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- ComboBox用のToggleButtonスタイル -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="20"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" Grid.ColumnSpan="2" Background="#3D3D3D"
                        BorderBrush="#555" BorderThickness="1" CornerRadius="3"/>
                <Path x:Name="Arrow" Grid.Column="1" Fill="White"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"/>
            </Grid>
        </ControlTemplate>

        <!-- ComboBox スタイル -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}"
                                          Focusable="False" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#3D3D3D" BorderBrush="#555" BorderThickness="1"/>
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3498DB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3498DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3498DB"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4A4A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- プリセット管理 -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="プリセット管理" Style="{StaticResource HeaderStyle}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ComboBox x:Name="PresetComboBox" Width="200" Margin="0,0,10,0"/>
                <Button Content="読み込み" Click="LoadPreset_Click" Margin="0,0,10,0"/>
                <Button Content="現在の設定を保存" Click="SavePreset_Click" Background="#27AE60" Margin="0,0,10,0"/>
                <Button Content="削除" Click="DeletePreset_Click" Background="#E74C3C"/>
            </StackPanel>
        </StackPanel>

        <!-- グリッドサイズ設定 -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="グリッドサイズ" Style="{StaticResource HeaderStyle}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="行数:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                <ComboBox x:Name="RowsComboBox" Width="70" Margin="0,0,30,0"
                          SelectionChanged="GridSize_Changed">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3" IsSelected="True"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                    <ComboBoxItem Content="7"/>
                    <ComboBoxItem Content="8"/>
                    <ComboBoxItem Content="9"/>
                    <ComboBoxItem Content="10"/>
                </ComboBox>
                <TextBlock Text="列数:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
                <ComboBox x:Name="ColsComboBox" Width="70"
                          SelectionChanged="GridSize_Changed">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4" IsSelected="True"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                    <ComboBoxItem Content="7"/>
                    <ComboBoxItem Content="8"/>
                    <ComboBoxItem Content="9"/>
                    <ComboBoxItem Content="10"/>
                </ComboBox>
                <TextBlock x:Name="GridInfoText" Text="" VerticalAlignment="Center"
                           Margin="20,0,0,0" FontSize="12" Foreground="#AAA"/>
            </StackPanel>
        </StackPanel>

        <!-- ボタン管理 -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="ボタン管理" Style="{StaticResource HeaderStyle}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="+ ボタン追加" Click="AddButton_Click" Margin="0,0,10,0"/>
                <Button Content="- 選択削除" Click="DeleteButton_Click" Background="#E74C3C"/>
            </StackPanel>
        </StackPanel>

        <!-- ボタン一覧と編集 -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ボタンリスト -->
            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                <TextBlock Text="ボタン一覧" Margin="0,0,0,8" FontWeight="Bold" FontSize="14"/>
                <ListBox x:Name="ButtonListBox" Height="320"
                         SelectionChanged="ButtonListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontSize="18" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding Label}" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>

            <!-- ボタン編集 -->
            <Border Grid.Column="1" Background="#2D2D2D" Padding="20" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="ボタン編集" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="ラベル:" Margin="0,0,0,5" FontSize="13"/>
                            <TextBox x:Name="LabelTextBox" Margin="0,0,0,15"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="アイコン (絵文字):" Margin="0,0,0,5" FontSize="13"/>
                            <TextBox x:Name="IconTextBox" Margin="0,0,0,15"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="アクションタイプ:" Margin="0,0,0,5" FontSize="13"/>
                    <ComboBox x:Name="ActionTypeComboBox" Margin="0,0,0,5"
                              SelectionChanged="ActionType_Changed">
                        <ComboBoxItem Content="ホットキー" Tag="Hotkey"/>
                        <ComboBoxItem Content="アプリ/URL起動" Tag="Open"/>
                        <ComboBoxItem Content="コマンド実行" Tag="Command"/>
                        <ComboBoxItem Content="テキスト入力" Tag="Text"/>
                    </ComboBox>
                    <TextBlock x:Name="ActionHelpText" Text="" Foreground="#AAA"
                               FontSize="12" TextWrapping="Wrap" Margin="0,5,0,12"/>

                    <TextBlock Text="アクション内容:" Margin="0,0,0,5" FontSize="13"/>
                    <TextBox x:Name="ActionTextBox" Height="50"
                             TextWrapping="Wrap" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"/>

                    <Button Content="このボタンを保存" Click="SaveButton_Click" Margin="0,12,0,0"
                            HorizontalAlignment="Left" Height="32" Padding="15,6"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- フッター -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="キャンセル" Click="Cancel_Click" Background="#666" Margin="0,0,10,0"/>
            <Button Content="全て保存して閉じる" Click="SaveAndClose_Click" Background="#27AE60"/>
        </StackPanel>
    </Grid>
</Window>
